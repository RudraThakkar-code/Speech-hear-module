version: '3.8'
services:
  db:
    image: postgres:14
    container_name: postgres_clinical
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: speech_clinical_dev
    ports:
      - "5432:5432"
